{
  "name": "create-expressr-app",
  "version": "1.1.8",
  "description": "Create Express apps with dynamic folder routing",
  "main": "bin/create-expressr-app.js",
  "bin": {
    "create-expressr-app": "./bin/create-expressr-app.js"
  },
  "files": [
    "bin/",
    "templates/",
    "addons/"
  ],
  "scripts": {
    "prepublishOnly": "node build.js -r && chmod +x bin/create-expressr-app.js",
    "build": "node build.js",
    "rebuild": "node build.js -r"
  },
  "keywords": [
    "express",
    "typescript",
    "routing",
    "create-app",
    "cli"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "prompt": "^1.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.4",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
